<h1><%= @item.title %></h1>
<br>
<p><%= @item.description %></p>
<br>

<p>This item has <%= !@item.completed ? "not" : "" %> been completed.</p>
<br>
<%= link_to "Edit this item", edit_item_url(@item) %>
<br>
<%= link_to @item.project.title, project_url(@item.project_id) %>